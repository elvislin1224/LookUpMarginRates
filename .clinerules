# 專案任務：台灣股票期貨保證金計算與即時查詢 App

## 1. 核心角色設定
- 你是一位精通 Next.js, TypeScript 與台灣金融市場規則的資深工程師。
- 你的目標是協助開發一個能精準計算「股票期貨保證金」的 App。

## 2. 專案架構規範
- **邏輯分離**：所有的保證金計算公式（包含 10 個核心欄位）必須獨立於 UI，存放在 `src/logic/` 目錄。
- **爬蟲規範**：
  - 目標網站為台灣期交所 (TAIFEX) 與證交所 (TWSE)。
  - 必須實作隨機延遲 (Random Delay) 以符合合法爬蟲禮節。
  - 抓取失敗時需有重試機制並記錄詳細 Log。

## 3. 金融邏輯準則
- **精確度要求**：保證金金額計算後，需根據期交所規則進行取整（通常是無條件捨去或四捨五入至整數）。
- **連動性**：當股價或保證金比例變動時，10 個相關計算欄位必須即時更新。

## 4. 開發溝通限制 (節省 Token)
- 在執行大規模修改前，請先輸出「實作計畫 (Plan)」供我審核。
- 優先使用 Prompt Caching，不要隨意刪除歷史對話脈絡。
- 如果發現專案中已存在類似功能，請優先重構而非重複撰寫。
## 5. 合約規格與計算邏輯
- **合約規格識別**：App 必須能識別並區分「標準型」與「小型」股票期貨。
  - 標準型股票期貨：一口等於 2000 股標的證券。
  - 小型股票期貨：一口等於 100 股標的證券。
- **計算公式規範**：
  - 實際保證金金額 = 股價 × 合約股數 (2000 或 100) × 保證金比例。
- **使用者操作流**：在試算介面中，必須提供切換「標準/小型」的選項，或自動根據爬取到的標代碼（例如小型合約通常有特定編碼）進行預設。
## 6. 自動化識別邏輯 (Contract Size Auto-Detection)
- **規則描述**：App 必須具備自動辨識「合約規格股數」的功能。
  - **條件 A**：若商品名稱（或從期交所爬取到的字串）包含「小型」二字，則 `contract_size` 自動設為 `100`。
  - **條件 B**：若商品名稱中未出現「小型」，則預設 `contract_size` 為 `2000`。
- **實作要求**：請寫一個獨立的工具函數（例如 `identifyContractSize(name: string)`）來處理此判斷，並確保這 10 個計算欄位皆引用此函數的結果。